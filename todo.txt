# Send Messages Feature - Refinements

## Mobile Interface
- [ ] Fix interface being too wide on mobile

## UX Improvements
- [ ] Show user message immediately in web interface when sending
  - Add message to UI optimistically on send
  - Replace with actual message when it comes through file watcher

## New Features
- [ ] Add "New Session" button
  - Should start in the same directory as the currently watched session
  - Opens a fresh Claude Code session

## CLI Options
- [x] Add `--dangerously-skip-permissions` option
  - Pass through to the `claude` CLI invocation
  - Avoids permission prompts when sending messages
